import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.focusfluxhub26717',
  appName: 'focus-flux-hub-26717',
  webDir: 'dist'
};

export default config;
